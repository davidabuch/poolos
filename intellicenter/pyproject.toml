[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-q --strict-markers --strict-config"
python_files = ["test_*.py"]

[tool.coverage.run]
branch = true
source = ["intellicenter/api"]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 90
